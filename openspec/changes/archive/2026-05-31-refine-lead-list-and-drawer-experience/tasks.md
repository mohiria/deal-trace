## 1. QA 先行与基线确认

- [x] 1.1 建立 `qa/lightweight-test-design.md`，覆盖列表搜索分页、新增线索入口复用、工作台 tab、抽屉阶段、顶部人员信息和指标卡。
- [x] 1.2 建立 `qa/regression-impact-analysis.md`，明确受影响视图、旧行为保留点、回归命令和非 TDD 视觉例外。
- [x] 1.3 审核现有 `DashboardView`、`MyLeadsView`、`PublicPoolView`、`CustomersView`、`UsersView`、`AppShell`、`LeadDetailPanel` 测试基线。
- [x] 1.4 在修改生产代码前，为可测试行为补充或更新 Red 测试；对纯样式视觉优化记录非 TDD 例外。

## 2. Red 测试覆盖

- [x] 2.1 更新工作台测试，覆盖 tab 顺序 `我的线索/公海线索/全部线索`、全部线索合并、搜索后分页、统一新增线索入口和指标卡视觉辅助移除。
- [x] 2.2 更新线索详情面板测试，覆盖阶段轨道只显示历史到当前、顶部不显示阶段标签。
- [x] 2.3 更新应用外壳测试，覆盖右上角不显示角色、今日提醒新增线索触发统一入口。
- [x] 2.4 更新我的线索和公海线索测试，覆盖搜索、分页、新增客户/新增线索入口和既有详情/认领行为。
- [x] 2.5 更新客户管理测试，覆盖去掉 `Table`/`Modal` 标签、分页、复用新建线索弹窗和查重阻断。
- [x] 2.6 更新用户管理测试，覆盖账号搜索、分页、创建 Sales 与自身停用入口不展示。
- [x] 2.7 运行新增/修改测试确认 Red 失败原因与预期行为差距一致，并记录证据。

## 3. 共享交互能力实现

- [x] 3.1 提取可复用 `CreateLeadModal`，保留现有客户选择、查重、历史流失、阻断、归属、校验和 `createLead` 调用。
- [x] 3.2 在工作台、应用外壳今日提醒、我的线索、公海线索、客户管理中接入统一新建线索弹窗。
- [x] 3.3 实现列表搜索分页派生逻辑，搜索或筛选变化时重置第一页，默认每页 10 条。

## 4. 页面体验实现

- [x] 4.1 调整销售工作台 tab、全部线索数据合并、分页、指标卡视觉和新增线索入口。
- [x] 4.2 调整 `LeadDetailPanel` 阶段展示和 `DashboardView` 抽屉宽度/关闭按钮位置。
- [x] 4.3 调整 `AppShell` 顶部人员信息与今日提醒新增线索入口。
- [x] 4.4 调整我的线索和公海线索页面，使其列表视觉、搜索、分页和操作区与工作台一致。
- [x] 4.5 调整客户管理页面，去掉无意义标签，保留客户搜索和新增客户行为，接入分页与统一新建线索。
- [x] 4.6 调整用户管理页面，增加搜索和分页，并保留账号创建、启停和权限行为。

## 5. 验证与闭环

- [x] 5.1 运行受影响 Vitest 测试并记录 Green 证据。
- [x] 5.2 运行 `openspec validate refine-lead-list-and-drawer-experience --strict`。
- [x] 5.3 运行 `pnpm exec vue-tsc -b` 和 `pnpm build`。
- [x] 5.4 条件允许时启动前端，用非无头浏览器检查工作台、抽屉、列表分页、统一新建线索入口和中文渲染。
- [x] 5.5 更新 QA 产物中的覆盖闭环、未覆盖测试点、失败分析、剩余风险和执行证据。
