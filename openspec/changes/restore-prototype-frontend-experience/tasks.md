## 1. QA 先行与基线确认

- [x] 1.1 建立 `qa/lightweight-test-design.md`，从 active spec、原型、现有代码、现有测试中提取测试点、测试层级、断言目标和覆盖产物。
- [x] 1.2 建立 `qa/regression-impact-analysis.md`，明确本次视觉/交互还原影响的旧行为、需要保留的回归测试和运行命令。
- [x] 1.3 对照 `prototype/dealtrace-workbench.html` 审核当前未提交的前端改动，记录保留、替换或迁移结论。
- [x] 1.4 确认待修改前端源码以 UTF-8 保存，并区分真实源码乱码与终端显示乱码。
- [x] 1.5 在修改生产代码前，为可测试的状态行为补充或更新 Red/可复用失败测试；对纯样式和浏览器视觉检查记录非 TDD 例外与替代验证。

## 2. 测试先行覆盖

- [x] 2.1 更新工作台组件测试，覆盖抽屉默认隐藏、点击线索打开、关闭清空选中态、tab 切换和筛选不改指标。
- [x] 2.2 更新线索详情面板测试，覆盖结束线索只读、系统日志不展示、操作入口按既有权限保留。
- [x] 2.3 更新客户管理测试，覆盖新建客户重复反馈、新建线索查重警告/阻断状态和既有 API 调用不变。
- [x] 2.4 更新用户管理测试，覆盖账号表格字段、角色/状态标签、新建 Sales 弹窗和自身停用入口不展示。
- [x] 2.5 先运行新增/修改测试并记录 Red 证据；若某项严格 TDD 不适用，写入 QA 产物的非 TDD 例外。

## 3. 外壳与工作台布局实现

- [x] 3.1 调整 `AppShell.vue`，按原型还原侧边栏、导航、品牌区和提醒卡片，同时保留 `visibleSections(auth.role)` 与现有路由。
- [x] 3.2 调整 `DashboardView.vue` 顶部区域和四个指标卡，使间距、信息密度、标签和 spark 视觉接近原型，且不改变 dashboard 数据语义。
- [x] 3.3 实现原型风格的工作台卡片：三个 tab、筛选区、紧凑表格、hover、选中态、标签、归属展示和操作按钮。
- [x] 3.4 确保 `我的线索`、`公海线索`、`本月结束` 仅使用现有 leads store 数据或前端派生结果。

## 4. 抽屉与线索详情实现

- [x] 4.1 让工作台详情抽屉默认不存在，仅在用户选择线索后打开，并提供显式关闭入口。
- [x] 4.2 抽屉关闭时清空 `activeLeadId` 和表格选中态，且不触发写请求。
- [x] 4.3 调整 `LeadDetailPanel.vue` 的抽屉表现：标题区、阶段轨道、基础字段网格、操作分组、进度时间线、空态和加载态。
- [x] 4.4 保留既有线索写入行为、角色可见性、后端错误兜底和结束线索只读行为。
- [x] 4.5 在后端系统日志读取能力交付前，不在抽屉中展示系统日志区或入口。

## 5. 真实交互状态映射实现

- [x] 5.1 调整客户管理页表现，使搜索、表格、新建客户弹窗、重复反馈、新建线索弹窗与原型视觉语言一致。
- [x] 5.2 调整客户页新建线索弹窗中的查重警告/阻断状态，使其对应原型交互示例。
- [x] 5.3 调整 `LeadDetailPanel.vue` 中赢单、流失、退回、分配、转移弹窗的表现，同时保留校验和 API 调用。
- [x] 5.4 调整用户管理页表现，使账号表格、角色/状态标签、新建 Sales 弹窗、启用/停用操作与共享视觉语言一致。

## 6. 验证与覆盖闭环

- [x] 6.1 运行新增/修改测试和直接受影响的前端单元/组件测试，并把执行证据写回 QA 产物。
- [x] 6.2 运行 `vue-tsc` 和前端构建。
- [x] 6.3 条件允许时启动前端，用非无头浏览器检查工作台布局、抽屉打开/关闭、客户/用户页面和中文渲染。
- [x] 6.4 更新 QA 产物中的覆盖闭环、未覆盖测试点、未解决阻塞和剩余风险。
- [x] 6.5 在最终说明中记录执行过的测试、未执行项、失败分析、回归范围和仍存在的视觉风险。
